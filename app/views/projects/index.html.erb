<h1>Portfolio</h1>

<table>
  <tr>
    <th>Title</th>
  </tr>

  <% @projects.each do |project| %>
    <tr>
      <td><%= project.title %> | <%= link_to 'Show', project_path(project) %></td>
    </tr>
  <% end %>

</table><br>

<%= link_to 'New Project', new_project_path %>
<a href="#">Project</a>
<a href="#">Blog</a>
<%= link_to 'About', about_path %>
<%= link_to 'Home', root_path %>